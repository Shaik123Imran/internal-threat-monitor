# Insider Threat Predictor - Requirements
# ===========================================
# Python dependencies for the Insider Threat Predictor project
#
# Installation:
# pip install -r requirements.txt
#
# After installation, download NLTK data for TextBlob (optional but recommended):
# python -c "import nltk; nltk.download('punkt'); nltk.download('brown')"

# Core GUI framework (usually included with Python)
# tkinter - Built-in Python library (no installation needed)

# Data processing and scientific computing
numpy>=1.20.0

# Machine learning for anomaly detection
scikit-learn>=1.0.0

# Natural language processing for sentiment analysis
textblob>=0.17.0

# Database - MongoDB for data persistence
pymongo>=4.0.0

# HTTP requests for URL data loading
requests>=2.28.0

# Note: TextBlob requires NLTK data packages:
# - punkt: Tokenizer models
# - brown: Corpus data for better sentiment analysis
# These are downloaded automatically on first use, but can be pre-downloaded
# The application will work without them but sentiment analysis may be limited
